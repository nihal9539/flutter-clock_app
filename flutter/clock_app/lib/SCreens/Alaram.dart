import 'package:flutter/material.dart';

class ScreenAlaram extends StatelessWidget {
  const ScreenAlaram({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('jdhdbhdj'),
    );
  }
}
